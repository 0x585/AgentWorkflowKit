## Managed Git Workflow

- This repository is managed by `{{ profile }}`.
- Full workflow rules: `./.workflow-kit/WORKFLOW_CONTRACT.md`.
- Workflow version: `{{ workflow_version }}`; source metadata: `.workflow-kit/source.json`.
