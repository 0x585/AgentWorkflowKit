# 1007

## 完成定义（DoD）

- [ ] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [ ] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

- 将 downstream `.git_scripts` / `.githooks` 从“本地 exclude 隐藏”改成“默认纳入版本控制”。
- 为 `AgentTask` 将默认分支配置改为 `main`，并确保默认分支探测优先遵循 repo manifest。
- 发布新的 workflow release，并用于下游仓库重新 apply。

## 变更文件

- `repos/AgentTask.json`
- `.git_scripts/git_default_branch.sh`
- `scripts/workflow_kit_lib.py`
- `docs/USAGE.md`
- `AGENTS.md`
- `tests/test_workflow_release.py`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.6/*`
- `profiles/full_codex_flow/releases/1.0.7/*`

## 变更说明

- `AgentTask` 的 `default_branch` 改为 `main`。
- `git_default_branch.sh` 改为优先采用 repo manifest 声明的默认分支，不再被陈旧的 `origin/HEAD` 带回旧分支。
- `apply_release` 不再向 downstream `.git/info/exclude` 写入 `.git_scripts/` / `.githooks/`，且会清理旧的 managed exclude block。
- 发布 `full_codex_flow@1.0.7`，供下游仓库同步。

## 验证结果

- `python3 -m compileall .git_scripts scripts tests`
- `PYTHONPATH=src/main/python python3 -m unittest tests.test_workflow_release`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.7`
- `python3 scripts/apply_release.py --repo-root /Users/pi/PyCharmProject/AgentTask --repo-id AgentTask`
- `python3 scripts/apply_release.py --repo-root /Users/pi/PyCharmProject/AgentTransitStation --repo-id AgentTransitStation`

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：downstream 从本地隐藏受管目录切换为版本控制后，后续仓库 diff 会显著增大。
- 回滚：回退本次 release 与 `workflow_kit_lib.py` / `git_default_branch.sh` 变更，重新发布上一版 workflow 并重新 apply downstream。
