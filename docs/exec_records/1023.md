# 1023

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 若有代码修改：已执行测试并记录结果
- [x] 若有代码修改：已完成变更审查并记录结论
- [ ] 若为代码任务：已 push / auto-release，并完成下游应用

## 需求摘要

- 修复 `public_work_register_sync.py` / `public_work_register_claim.py` 模板中 `PACKAGE_ROOT` 被写死为中央仓包名的问题。
- 发布 `full_codex_flow@1.0.23`，让下游 `public_work_register_*` 脚本跟随 repo manifest 的 `python_package_name` 展开。

## 变更文件

- `AGENTS.md`
- `docs/USAGE.md`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.23/*`
- `scripts/workflow_kit_lib.py`
- `templates/full_codex_flow/files/.workflow-kit/public_work_register_sync.py.tmpl`
- `templates/full_codex_flow/files/.workflow-kit/public_work_register_claim.py.tmpl`
- `tests/test_workflow_release.py`

## 变更说明

- 在 `placeholderize_runtime_entry` 中新增 `PACKAGE_ROOT` 的占位符回写逻辑，避免从中央仓 runtime 导出模板时把包路径写死成 `agent_workflow_kit`。
- 补充中央仓文档，明确 `public_work_register_*` 入口脚本的包路径必须来自 repo manifest 的 `python_package_name`。
- 运行 `export_templates` 回写模板，发布 `full_codex_flow@1.0.23`，并生成新的 release/lock 工件。
- 补充测试，验证导出的 `public_work_register_sync.py.tmpl` 同时对 import 语句和 `PACKAGE_ROOT` 保留 `{{ python_package_name }}` 占位符。

## 验证结果

- 命令：`ASSERT_PURPOSE=code ./.workflow-kit/assert_workspace.sh`；`python3 scripts/export_templates.py --repo-id AgentWorkflowKit`；`python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.23`；`python3 -m pytest tests/test_workflow_release.py`
- 范围：runtime 导出占位符恢复、模板产物、release 工件生成、完整 workflow release/apply 测试链路。
- 结果：`export_templates` 成功导出 25 个模板；`publish_release` 成功生成 `1.0.23` release；`tests/test_workflow_release.py` 33 个用例全部通过。
- 未覆盖项：尚未在下游仓库完成 `1.0.23` 的 commit/push；下游 apply 结果将在中央仓 auto-release 后继续验证。
- 提交快照：05223bfb879e8bc995627d203583aff08b1a9d003a8621c08125603afd14aeb6

## 审查结果

- 审查方式：人工审查 `git diff --stat`、模板 grep 校验、release 版本变更复核。
- 结论：修复点集中且闭环完整；模板、导出逻辑、release 工件和测试已经对齐。
- 残余风险：下游仓库仍需要应用 `1.0.23` 才能消除已生成的错误兼容目录；中央仓提交后应继续检查 `AgentTask` / `AgentTransitStation` 的 fan-out 结果。
- 提交快照：05223bfb879e8bc995627d203583aff08b1a9d003a8621c08125603afd14aeb6

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 若 `1.0.23` 下游 apply 出现异常，可先回滚中央仓本次提交，再重新发布上一稳定版本。
- 若只需回退模板修复，可回退 `scripts/workflow_kit_lib.py`、对应模板和 release 工件，然后重新发布新 patch 版本覆盖。
