# 1026

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 若有代码修改：已执行测试并记录结果
- [x] 若有代码修改：已完成变更审查并记录结论
- [ ] 若为代码任务：已 push / auto-release，并完成下游应用

## 需求摘要

放宽中央 workflow commit gate 对执行记录结构化字段的格式限制：`验证结果` 中的 `命令`、`范围`、`结果`、`未覆盖项` 在保留字段头的前提下，允许把正文续写到后续多行，不再强制单行；同时保持 `提交快照` 校验、模板导出和 release 工件同步。

## 变更文件

- `.workflow-kit/exec_record_hygiene.py`
- `.workflow-kit/WORKFLOW_CONTRACT.md`
- `templates/full_codex_flow/files/.workflow-kit/exec_record_hygiene.py.tmpl`
- `templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`
- `tests/test_workflow_release.py`
- `AGENTS.md`
- `docs/USAGE.md`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.26/`
- `docs/exec_records/INDEX.md`
- `docs/exec_records/1026.md`
- `docs/exec_records/1026_commit.txt`

## 变更说明

- 调整 `exec_record_hygiene.py` 的结构化 section 解析逻辑：字段值现在会一直读取到下一个受管字段头为止，因此同一字段下可以继续写多行说明；`sync_staged_snapshot` 也会整块替换对应字段，避免旧续行残留。
- 新增 `test_commit_msg_allows_multiline_validation_fields` 回归测试，并修正测试 helper 的执行记录写法，让多行字段场景与真实 markdown 布局一致。
- 更新中央 contract / runbook 文案，明确“保留字段头、正文可多行”的规则；随后导出模板并发布 `full_codex_flow@1.0.26`。

## 验证结果

- 命令：`python3 -m py_compile .workflow-kit/exec_record_hygiene.py tests/test_workflow_release.py scripts/workflow_kit_lib.py`
  - `python3 -m unittest tests.test_workflow_release.WorkflowReleaseTest.test_commit_msg_allows_multiline_validation_fields`
  - `python3 -m unittest tests.test_workflow_release`
  - `python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
  - `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.26`
  - `git diff --check`
- 范围：exec record 结构化字段解析与快照回写
  - commit-msg 代码任务 gate 的多行字段兼容性
  - 中央 contract / runbook、模板导出与 release `1.0.26` 工件
- 结果：全部通过
  - 定向回归 `test_commit_msg_allows_multiline_validation_fields` 通过
  - `tests.test_workflow_release` 全量 36 个用例通过
  - `export_templates` 成功导出 25 个模板
  - `publish_release` 成功生成 `full_codex_flow@1.0.26`
  - release manifest hash：`2adffdf5de016c02933c7f891cc2b2a93e44b456911966bb8675ea0c139fdaca`
  - `git diff --check` 无输出
- 未覆盖项：未额外在真实下游仓库执行 `apply_downstreams.py` fan-out / auto-release 端到端演练
  - 本次验证集中在中央 hook、模板与 release 工件的一致性
- 提交快照：02edcbcc9a489cf881310b058d2b87a8f617853170b793b223627866db942c7a
## 审查结果

- 审查方式：人工审查 `git diff`、`exec_record_hygiene.py` 的字段分段逻辑、模板导出结果与新增回归测试
- 结论：实现与需求一致；在继续要求字段头存在且 `提交快照` 必须精确匹配的前提下，`验证结果` / `审查结果` 的字段正文现在可以安全写成多行，中央模板与 release 工件也已同步到 `1.0.26`
- 残余风险：尚未应用 `1.0.26` 的下游仓库仍会保留旧 runtime；只有在后续 apply 当前 release 后，才会获得多行字段兼容能力
- 提交快照：02edcbcc9a489cf881310b058d2b87a8f617853170b793b223627866db942c7a
## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：本次修改改变了 commit gate 对执行记录 markdown 的解析范围；若后续有人在字段正文里意外写出新的受管字段头，解析仍会在该处截断。
- 回滚：回退本次 `exec_record_hygiene` / 模板 / 文档 / 测试 / release `1.0.26` 相关提交，并重新发布上一有效 workflow 版本。
