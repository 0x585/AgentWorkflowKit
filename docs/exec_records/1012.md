# 1012

## 完成定义（DoD）

- [x] 需求目标已明确（恢复 `1.0.8` release 历史，并修复 downstream apply 在旧 runtime 下因共享 `.venv` 链接导致的 worktree 脏状态）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

修复中央仓库缺失 `full_codex_flow@1.0.8` release 历史导致下游仓库 `check_release/apply_downstreams` 直接 `invalid` 的问题，并补齐 downstream apply 对旧版共享 `.venv` runtime 的兼容启动路径。最终重新发布 `1.0.10`，并验证中央 fan-out 能在两个下游仓库中生成本地 worktree commit。

## 变更文件

- `.git_scripts/ensure_shared_venv.sh`
- `scripts/workflow_kit_lib.py`
- `tests/test_workflow_release.py`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.10/*`
- `templates/full_codex_flow/files/.git_scripts/ensure_shared_venv.sh.tmpl`
- `AGENTS.md`
- `docs/USAGE.md`

## 变更说明

- 让 `ensure_shared_venv.sh` 在 worktree 级 `.git/info/exclude` 中登记共享 `.venv` 链接，避免 `post-checkout` / `new_exec` 的自动 sync 因未跟踪软链接而误判 dirty。
- 让中央 `apply_downstreams.py` 在调用旧版下游 `new_worktree.sh` 时临时设置 `SKIP_SHARED_VENV_LINK=1`，待当前 release 应用完成后再由新 runtime 补做共享 `.venv` 修复。
- 重新导出模板并发布 `full_codex_flow@1.0.10`，然后完成真实下游 fan-out 验证。

## 验证结果

- `python3 -m compileall .git_scripts scripts tests`
- `python3 -m unittest tests.test_workflow_release`
- `python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.10`
- `python3 scripts/apply_downstreams.py`

## 完成待办项

- 无

## 当前占用待办项

- `AgentTask-wt-workflow-release-1-0-10` 已生成本地 commit `ae24718673b72438913ab375da7f5129d9c4a4f7`，后续如需真正入库仍需在子仓库自行 push/release。
- `AgentTransitStation-wt-workflow-release-1-0-10` 已生成本地 commit `e09501de4fb19d5a2313e6bfc94364aba498f143`，后续如需真正入库仍需在子仓库自行 push/release。

## 风险与回滚

- 风险：若后续在子仓库重复创建同名 `workflow-release-1-0-10` 分支/worktree，会按设计返回冲突；另外历史遗留的 `*-workflow-release-1-0-9` worktree 仍需人工决定是否清理。
- 回滚：回退本次中央仓库 commit，并恢复 `profiles/full_codex_flow/release.json` 与 `profiles/full_codex_flow/releases/1.0.10/` 前的状态；如需撤销下游结果，再分别删除子仓库的本地 worktree/branch。
