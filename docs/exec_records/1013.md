# 1013

## 完成定义（DoD）

- [x] 需求目标已明确（将 `.git_scripts` 入口迁移到 `.workflow-kit`，并把 workflow 文档收束到 `AGENTS.md` / `README.md` 固定区域）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

- 将受管 workflow runtime 的 canonical 入口从 `.git_scripts/` 迁移到 `.workflow-kit/`
- 为旧子仓保留 `.git_scripts/*` 兼容 shim，并允许中央 fan-out 在首次迁移时回退到旧入口创建 worktree
- 把通用 workflow 文档收束到 `AGENTS.md` / `README.md` 的固定 managed block，并在子仓 apply 后刷洗散落的旧 workflow 章节

## 变更文件

- `.workflow-kit/`、`.git_scripts/`、`.githooks/`
- `scripts/workflow_kit_lib.py`
- `profiles/full_codex_flow/`
- `templates/full_codex_flow/blocks/`
- `templates/full_codex_flow/files/.workflow-kit/`
- `templates/full_codex_flow/files/.git_scripts/`
- `AGENTS.md`
- `README.md`
- `docs/USAGE.md`
- `tests/test_workflow_release.py`

## 变更说明

- 将中央仓库真实 runtime 源迁移到 `.workflow-kit/`，并把 `.git_scripts/*` 改为委托到 `.workflow-kit/*` 的兼容入口
- 更新 profile/模板导出与 release apply 逻辑，让下游同时生成 `.workflow-kit/*` 和 `.git_scripts/*` shim
- 为 `apply_downstreams.py` 增加对旧子仓 `.git_scripts/new_worktree.sh` 的 fallback，保证老仓也能收到首次迁移版本
- 在 downstream apply 时刷新 `scripts/*` wrapper，并对刷洗 `AGENTS.md` / `README.md`，把通用 workflow 说明收束到固定 managed block
- 扩充并修正单测，覆盖占位符导出、兼容入口、文档刷洗与 downstream worktree commit 流程

## 验证结果

- `python3 -m compileall .workflow-kit .git_scripts .githooks scripts tests`
- `python3 -m unittest tests.test_workflow_release`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.14`
- `python3 scripts/apply_downstreams.py`
- 下游结果：
  - `AgentTask-wt-workflow-release-1-0-14` / commit `ee3e5391fb5591b2a0083cd6ff6f4acd2c52d238`
  - `AgentTransitStation-wt-workflow-release-1-0-14` / commit `420ce50e25834e54cf72735ebb7e243d4a5f2ac0`

## 完成待办项

- 无

## 当前占用待办项

- 本分支尚未 merge / release；子仓 `1.0.14` worktree commit 也仍停留在本地，待后续人工检查并决定是否 push/release

## 风险与回滚

- 风险：当前子仓主 checkout 仍停留在旧安装版本，`1.0.14` 结果存在于新建 worktree commit 中，尚未进入各自默认分支
- 回滚：删除对应 `*-wt-workflow-release-1-0-14` worktree 与本地分支，中央仓库回退本地提交或丢弃当前 worktree 变更
