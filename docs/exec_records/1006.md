# 1006

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

修正下游仓库受管文档 block 的文案。

问题表现：

- workflow 应用到子项目后，`AGENTS.md` / `README.md` 的受管 block 会直接写出 `AgentWorkflowKit`
- 这会把中央仓库项目名暴露到子项目文档里，阅读体验也容易误导成“当前仓库名就是 AgentWorkflowKit”

本次目标：

- 子项目 `AGENTS.md` / `README.md` 的受管 block 不再直接写 `AgentWorkflowKit`
- 仍保留 profile、version 与受管说明
- 发布新版本并应用到下游仓库

## 变更文件

- `templates/full_codex_flow/blocks/agents-workflow.md.tmpl`
- `templates/full_codex_flow/blocks/readme-workflow.md.tmpl`
- `tests/test_workflow_release.py`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/releases/1.0.5/*`
- `README.md`
- `AGENTS.md`
- `docs/exec_records/INDEX.md`
- `docs/exec_records/1006.md`
- `docs/exec_records/1006_commit.txt`

## 变更说明

- 将受管 block 改为通用表述，不再在下游文档中直接写中央项目名
  - `AGENTS.md` block 现在改为 `Managed Workflow Release`
  - `README.md` block 改为展示 `profile`、`workflow_version` 与 `.workflow-kit/source.json`
- 去掉会在下游文档中直接出现 `AgentWorkflowKit` 或中央仓库路径的行
  - 移除 `Managed by AgentWorkflowKit`
  - 移除 `Central repo: /Users/pi/PyCharmProject/AgentWorkflowKit`
  - README 中的手工 resync 改为通用说明，不直接内嵌中央仓库路径
- 新增回归测试，确保下游渲染出的 `AGENTS.md` / `README.md` block 不再包含 `AgentWorkflowKit`
- 发布 `full_codex_flow@1.0.5` 并应用到中央仓库、`AgentTask`、`AgentTransitStation`

## 验证结果

- `python3 -m unittest tests/test_workflow_release.py` 通过（`9 tests`）
- `python3 -m compileall scripts tests` 通过
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.5` 通过
- `python3 scripts/apply_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-downstream-agents-block --repo-id AgentWorkflowKit` 通过
- `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-downstream-agents-block --repo-id AgentWorkflowKit --json` 返回 `status: current`
- `python3 scripts/apply_downstreams.py` 通过，并将 `AgentTask`、`AgentTransitStation` 应用到 `1.0.5`
- `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentTask --repo-id AgentTask --json` 返回 `status: current`
- `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentTransitStation --repo-id AgentTransitStation --json` 返回 `status: current`
- 抽查 `AgentTask/AGENTS.md`、`AgentTransitStation/AGENTS.md` 与 `AgentTask/README.md`，受管 block 已不再包含 `AgentWorkflowKit`

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：
  - 这次只调整了受管文档 block 文案，没有改动 workflow 运行时逻辑；如果后续需要在子项目文档里恢复明确的“中央源仓库路径”，需要重新设计一套不误导的表达
- 回滚：
  - 回退两个 block 模板与测试
  - 重新发布一个恢复旧文案的 workflow 版本并重新应用到下游
