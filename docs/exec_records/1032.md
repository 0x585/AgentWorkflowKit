# 1032

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 若有代码修改：已执行测试并记录结果
- [x] 若有代码修改：已完成变更审查并记录结论
- [ ] 若为代码任务：已 push / auto-release，并完成下游应用

## 需求摘要

workflow conflict guidance docs

## 开工计划

- 工作类型：续作(codex/workflow-conflict-guidance/1032)
- 目标：把“冲突先自行处理非业务逻辑部分，只有逻辑/语义冲突才停下人工确认”的规则写进受管 workflow 文档，让下游仓的 Codex 默认按这条指令执行。
- 改动范围：`templates/full_codex_flow/blocks/agents-workflow.md.tmpl`、`.workflow-kit/WORKFLOW_CONTRACT.md`、`templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`、`AGENTS.md`、`docs/USAGE.md`、`tests/test_workflow_release.py`、release 导出产物与 `profiles/full_codex_flow/releases/1.0.33/*`
- 实施步骤：1. 更新 managed `AGENTS` block 和 contract，补充允许自动处理与必须人工确认的冲突分类，以及 source rebase / primary merge 的继续动作。2. 同步中央维护文档与 release 测试断言。3. 导出模板、发布 `1.0.33` release，并完成验证、审查、提交与下游应用。
- 预期验证：运行 `python3 -m unittest tests.test_workflow_release`、`python3 scripts/export_templates.py --repo-id AgentWorkflowKit`、`python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.33`、`git diff --check`
- 已知风险/阻塞：这次只改文档/指令层，不下沉 shell 自动解冲突能力；中央源码仓 worktree 仍可能被 release guard 误判为 `drift`，必要时继续用 `WORKFLOW_GUARD_ACTIVE=1` 跑受管入口。

## 变更文件

- `.workflow-kit/WORKFLOW_CONTRACT.md`
- `AGENTS.md`
- `docs/USAGE.md`
- `templates/full_codex_flow/blocks/agents-workflow.md.tmpl`
- `templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`
- `tests/test_workflow_release.py`
- `profiles/full_codex_flow/{release.json,managed-files.lock.json,releases/1.0.33/*}`

## 变更说明

- 受管 `AGENTS` block 新增一条高优先级冲突处理指令：遇到 sync / merge / release 冲突时，先自行解决明显的机械性冲突，只有代码逻辑或语义冲突才停下人工确认。
- managed contract 新增 `Conflict Handling` 章节，明确可默认处理的非业务逻辑冲突清单，以及 source rebase / primary merge 两类恢复动作。
- 中央 `AGENTS.md` 与 `docs/USAGE.md` 同步维护者口径，并发布 `full_codex_flow@1.0.33` 让下游仓通过受管文档收到这条规则。

## 验证结果

- 命令：`python3 -m unittest tests.test_workflow_release`
- 范围：workflow 文档渲染、apply release、managed blocks 注入与 autorelease 回归
- 结果：通过，`Ran 51 tests in 170.013s`
- 未覆盖项：不覆盖下游仓在真实冲突场景中的人工处理质量，只验证受管文档与发布流程
- 命令：`python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
- 范围：runtime -> template 导出，确保 `.workflow-kit/WORKFLOW_CONTRACT.md` 的文案同步到模板
- 结果：通过，`exported_template_count=29`
- 未覆盖项：不覆盖 release artifact 写入
- 命令：`python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.33`
- 范围：生成 `full_codex_flow@1.0.33` release manifest 与 lock
- 结果：通过，`release_manifest_hash=26443dc68ec489166c52be90afbf188a607c86ef0e02a97fb884d946a1a49eeb`
- 未覆盖项：仅覆盖中央 release 产物生成，不替代提交后的 downstream apply
- 命令：`git diff --check`
- 范围：本次文档、测试与 release 产物 patch 格式检查
- 结果：通过
- 未覆盖项：不覆盖运行时语义
- 提交快照：991b2059be80d308a680bca037ca64667fcc7afe06ce39f8cac4d6f84aedbf10
## 审查结果

- 审查方式：人工自审 managed `AGENTS` block、contract、central `AGENTS` / `USAGE` 文案与对应测试断言，确认中央源仓说明与下游模板保持一致
- 结论：当前变更把“先自行解决非业务逻辑冲突、逻辑/语义冲突再人工确认”的规则同时写进入口摘要与详细 contract，覆盖中央与下游受管文档
- 残余风险：这次只改指令层，不会让 shell runtime 自动解冲突；真实行为仍依赖 Codex 读取这些受管文档后按规则执行
- 提交快照：991b2059be80d308a680bca037ca64667fcc7afe06ce39f8cac4d6f84aedbf10
## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：新的冲突处理指令会传播到所有 `full_codex_flow` 下游仓，可能改变 Codex 在冲突场景下的默认停手时机。
- 回滚：回退本次中央源码、模板与 `1.0.33` release 提交，并重新发布上一版 release 覆盖下游文档。
