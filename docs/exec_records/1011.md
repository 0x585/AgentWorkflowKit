# 1011

## 完成定义（DoD）

- [x] 需求目标已明确（恢复 `1.0.8` release 历史并把当前 release 提升到 `1.0.9`）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

修复中央仓库缺失 `full_codex_flow@1.0.8` release 历史导致下游仓库 `check_release/apply_downstreams` 直接 `invalid` 的问题。在保留 `1009` 的 downstream-local-commit 行为基础上，接回漏合并的 `1008` 工作流能力，并重新发布 `1.0.9`。

## 变更文件

- `.git_scripts/ensure_shared_venv.sh`
- `.git_scripts/new_exec.sh`
- `.git_scripts/new_worktree.sh`
- `.githooks/post-checkout`
- `scripts/workflow_kit_lib.py`
- `tests/test_workflow_release.py`
- `profiles/full_codex_flow/profile.json`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.8/*`
- `profiles/full_codex_flow/releases/1.0.9/*`
- `AGENTS.md`
- `docs/USAGE.md`

## 变更说明

- 将漏掉的 `1008` workflow/runtime 变更接回当前主线工作区，包括共享 `.venv` 与 worktree 辅助逻辑。
- 恢复 `full_codex_flow@1.0.8` release 历史，使下游已安装 `1.0.8` 的仓库可以被正确识别为 `outdated` 而不是 `invalid`。
- 在当前主线状态上重新导出模板并发布 `full_codex_flow@1.0.9`，保证 `1.0.8 -> 1.0.9` 升级链完整。

## 验证结果

- `python3 -m compileall .git_scripts scripts tests`
- `python3 -m unittest tests.test_workflow_release`
- `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentTask --repo-id AgentTask --json`
- `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentTransitStation --repo-id AgentTransitStation --json`
- `python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.9`

## 完成待办项

- 无

## 当前占用待办项

- 中央仓库本次提交后的 `post-commit` 将实际为下游仓库生成 `1.0.9` 本地 worktree 提交。

## 风险与回滚

- 风险：若下游仓库已经存在同名 `workflow-release-1-0-9` 分支/worktree，本次真实下推会按设计返回失败并继续其他仓库。
- 回滚：回退本次中央仓库 commit，并恢复 `profiles/full_codex_flow/release.json` 与 `profiles/full_codex_flow/releases/1.0.8/`、`profiles/full_codex_flow/releases/1.0.9/` 前的状态。
