# 1022

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 若有代码修改：已执行测试并记录结果
- [x] 若有代码修改：已完成变更审查并记录结论
- [ ] 若为代码任务：已 push / auto-release，并完成下游应用

## 需求摘要

在中央仓实现 workflow 文案瘦身：新增统一下推的 `./.workflow-kit/WORKFLOW_CONTRACT.md` 作为完整合同，把下游 `AGENTS.md` / `README.md` managed block 缩成短入口，并在 release/apply 检查中追加对块外重复 workflow 文案的非阻断 warning，随后发布新 release `1.0.22`。

## 变更文件

- `.workflow-kit/WORKFLOW_CONTRACT.md`
- `profiles/full_codex_flow/profile.json`
- `templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`
- `templates/full_codex_flow/blocks/agents-workflow.md.tmpl`
- `templates/full_codex_flow/blocks/readme-workflow.md.tmpl`
- `scripts/workflow_kit_lib.py`
- `scripts/check_release.py`
- `tests/test_workflow_release.py`
- `AGENTS.md`
- `README.md`
- `docs/USAGE.md`
- `.workflow-kit/*`
- `.githooks/*`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.22/`
- `docs/exec_records/INDEX.md`
- `docs/exec_records/1022.md`
- `docs/exec_records/1022_commit.txt`

## 变更说明

- 为 `full_codex_flow` 新增受管文件 `./.workflow-kit/WORKFLOW_CONTRACT.md`，把完整 workflow 规则固定收敛到单一真源，并在中央仓 source/runtime 与模板两侧同步落地。
- 将 `AGENTS.md` / `README.md` managed block 改成短入口，分别只保留执行必需提示和 contract 指针，同时把中央仓自身 `AGENTS.md`、`README.md`、`docs/USAGE.md` 的重复 workflow 入口文案收短。
- 在 `scripts/workflow_kit_lib.py` 增加只扫描 `README.md` / `AGENTS.md` 块外文本的重复文案检查，并把 `doc_redundancy_warnings` 追加到 `check_release` / `apply_release` summary。
- 补充 release/apply/export 相关测试，覆盖 contract 文件生成、短 block 渲染、warning 正负例，以及 CLI summary 输出兼容字段。
- 执行 `export_templates`、发布 release `1.0.22`，并刷新中央仓当前 worktree 的受管文件版本。

## 验证结果

- 命令：`python3 -m py_compile scripts/workflow_kit_lib.py scripts/check_release.py scripts/apply_release.py tests/test_workflow_release.py`；`python3 -m unittest tests.test_workflow_release`；`python3 scripts/export_templates.py --repo-id AgentWorkflowKit`；`python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.22`；`python3 scripts/apply_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-workflow-contract-trim --repo-id AgentWorkflowKit`；`python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-workflow-contract-trim --repo-id AgentWorkflowKit --json`；在 `AgentTask` / `AgentTransitStation` detached worktree 上各执行一次 `python3 scripts/apply_release.py ...` 与 `python3 scripts/check_release.py --json ...`；`git diff --check`
- 范围：contract 文件下推、managed block 缩短、块外重复文案 warning、中央仓文档收短、release `1.0.22` 工件与抽样下游 apply/check 验证
- 结果：全部通过；`tests.test_workflow_release` 共 33 个用例通过；中央仓 `check_release` 为 `current` 且无 warning；`AgentTask` / `AgentTransitStation` 的 detached worktree 抽样 apply 后均 `check_status=current`
- 未覆盖项：未直接在 `AgentTask` / `AgentTransitStation` 现有主工作区执行 apply，因为两仓当前已有未提交改动；抽样验证改为在 detached worktree 中完成
- 提交快照：c136ea4e165ea20f8225e52fe4cdb0774917a90ad69d6deff06b002867b0ec64

## 审查结果

- 审查方式：人工审查 `git diff --stat` 与关键文件 diff，重点检查 contract 内容是否成为单一详细真源、warning 只扫描块外 `README.md` / `AGENTS.md`、模板导出后的 placeholder 是否保留，以及中央 runtime/hook 文件是否仅发生版本号与 release hash 回写
- 结论：实现与方案一致；managed block 已显著缩短，完整规则集中到 `WORKFLOW_CONTRACT.md`，`doc_redundancy_warnings` 只做兼容性追加且不影响 release/apply 主流程，未发现行为语义回归
- 残余风险：块外重复文案 warning 目前依赖固定 topic 关键字匹配；后续若各仓出现新的重复写法，可能需要补充 topic pattern 才能继续稳定暴露
- 提交快照：c136ea4e165ea20f8225e52fe4cdb0774917a90ad69d6deff06b002867b0ec64

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：真实下游仓库的主工作区当前并不干净，若后续直接在原路径 apply，仍需先评估现有改动如何收口。
- 回滚：回退本次 contract / 模板 / 脚本 / 文档 / release `1.0.22` 相关提交，并重新发布上一版本 release。
