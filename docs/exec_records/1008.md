# 1008

## 完成定义（DoD）

- [x] 需求目标已明确（本次只做本地提交，不直接 merge 到 main）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

将此前“把 `.venv` 纳入版本控制”的做法改为“`.git_scripts` / `.githooks` 继续纳入版本控制，但 `.venv` 保持本地运行时，通过受管 workflow 在 worktree 中自动链接主仓共享环境”。

## 变更文件

- `.git_scripts/ensure_shared_venv.sh`
- `.git_scripts/new_worktree.sh`
- `.git_scripts/new_exec.sh`
- `.githooks/post-checkout`
- `profiles/full_codex_flow/profile.json`
- `templates/full_codex_flow/files/.git_scripts/ensure_shared_venv.sh.tmpl`
- `templates/full_codex_flow/files/.git_scripts/new_worktree.sh.tmpl`
- `templates/full_codex_flow/files/.git_scripts/new_exec.sh.tmpl`
- `templates/full_codex_flow/files/.githooks/post-checkout.tmpl`
- `scripts/workflow_kit_lib.py`
- `tests/test_workflow_release.py`
- `AGENTS.md`
- `docs/USAGE.md`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.8/*`

## 变更说明

- 新增受管脚本 `ensure_shared_venv.sh`，支持从主仓自动为 worktree 修复/创建共享 `.venv` 软链接。
- 将 `new_worktree`、`new_exec` 与 `post-checkout` 接入共享 `.venv` 修复逻辑，避免 worktree 缺解释器时只能靠把 `.venv` 入库解决。
- 更新中央 profile、模板导出、release 工件、测试与维护文档，正式把 `full_codex_flow` 发布到 `1.0.8`。

## 验证结果

- `python3 -m compileall .git_scripts scripts tests`
- `PYTHONPATH=src/main/python python3 -m unittest tests.test_workflow_release`
- `python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.8`

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 风险：worktree 侧 `.venv` 现在依赖主仓共享环境，若主仓环境损坏会同时影响关联 worktree。
- 回滚：恢复旧 release，或移除共享链接逻辑并重新发布；worktree 本地旧 `.venv` 已在各子项目 `.cache/workflow-kit/shared-venv-backups/` 中备份。
