# 1009

## 完成定义（DoD）

- [x] 需求目标已明确（本次实现“下游 apply 变为子仓本地 worktree 提交”，不直接 push/release 子仓）
- [x] 变更验证命令已执行并记录
- [ ] 若为代码任务：已合并到目标分支并完成分支清理

## 需求摘要

将中央仓库的 `scripts/apply_downstreams.py` 从“直接改写下游主工作区”调整为“为每个需要更新的子仓创建本地 worktree、应用当前 release、生成 exec 记录并创建本地 commit”。同时保留 `apply_release.py` 与下游 `workflow_guard` 的原有直写行为不变。

## 变更文件

- `scripts/workflow_kit_lib.py`
- `scripts/apply_downstreams.py`
- `tests/test_workflow_release.py`
- `AGENTS.md`
- `docs/USAGE.md`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.6/*`

## 变更说明

- 新增下游 worktree 提交 helper，负责检查 release 状态、调用子仓 `new_worktree.sh`、应用 release、重写 exec 记录、创建本地 commit，并在无实际 managed diff 时自动清理 worktree。
- `apply_downstreams.py` 改为输出 commit-aware JSON 摘要；遇到单仓失败时继续处理其他仓库，并最终返回非零退出码。
- 新增覆盖 `current`、`outdated`、脏主仓、noop cleanup、分支冲突、部分失败的集成测试。
- 更新维护文档，并发布 `full_codex_flow@1.0.7` release 工件。

## 验证结果

- `python3 -m compileall scripts tests`
- `python3 -m unittest tests.test_workflow_release`
- `python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
- `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.7`

## 完成待办项

- 无

## 当前占用待办项

- 下游子仓的真实 `apply_downstreams.py` 运行会在中央仓库提交后由 hook 触发；本次未提前对真实子仓执行，避免在提交前制造重复的 downstream worktree。

## 风险与回滚

- 风险：如果某个子仓已经存在同名 `workflow-release-*` 分支/worktree，本次下推会按设计报告失败并继续其他仓库。
- 回滚：回退本次中央仓库 commit，并恢复 `profiles/full_codex_flow/release.json` 与 `profiles/full_codex_flow/releases/1.0.7/` 前的 release 状态。
