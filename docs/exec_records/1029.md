# 1029

## 完成定义（DoD）

- [x] 需求目标已明确（含“是否必须 merge 到 main 并删除分支”）
- [x] 若有代码修改：已执行测试并记录结果
- [x] 若有代码修改：已完成变更审查并记录结论
- [ ] 若为代码任务：已 push / auto-release，并完成下游应用

## 需求摘要

document branch naming rule

## 开工计划

- 工作类型：新需求
- 目标：把“`codex/*` 分支名必须清楚描述用途、避免使用 `help` / `fix` 等模糊词”的规则写入中央仓文档与受管合同，并发布新 release 让下游仓库后续 apply 时继承；完成后应 merge 到 `main` 并删除任务分支。
- 改动范围：`AGENTS.md`、`docs/USAGE.md`、`./.workflow-kit/WORKFLOW_CONTRACT.md`，以及 `export_templates` / `publish_release` 生成的受管模板与 release 工件。
- 实施步骤：1. 先把当前 worktree 的受管 runtime 从已安装的 `1.0.22` 同步到当前 release `1.0.29`，解除 `start_exec` / commit gate 的漂移阻塞。2. 更新中央仓文档与受管合同，明确要求任务分支名必须表达具体用途。3. 执行 `python3 scripts/export_templates.py --repo-id AgentWorkflowKit` 与 `python3 scripts/publish_release.py --profile full_codex_flow --version <new-version>`，生成会传播到下游的 release。4. 完成验证、审查，并按受管流程提交。
- 预期验证：`python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-document-branch-naming --repo-id AgentWorkflowKit --json` 显示当前 release 状态正常；`python3 scripts/export_templates.py --repo-id AgentWorkflowKit` 成功；`python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.30` 成功；必要时用 `rg` / `git diff --check` 确认规则文本已写入中央文档和受管合同。
- 已知风险/阻塞：仓库当前主线已发布 release 为 `1.0.29`，但工作树原先仍停在 `1.0.22`，需要先补齐这部分受管文件更新；这会让本次提交同时包含基线同步和新规则落地。

## 变更文件

- `AGENTS.md`
- `docs/USAGE.md`
- `./.workflow-kit/WORKFLOW_CONTRACT.md`
- `templates/full_codex_flow/blocks/agents-workflow.md.tmpl`
- `templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`
- `profiles/full_codex_flow/release.json`
- `profiles/full_codex_flow/managed-files.lock.json`
- `profiles/full_codex_flow/releases/1.0.30/`
- `./.workflow-kit/install.json`
- `README.md`
- 其余受管 `.workflow-kit/*` 与 `.githooks/*` 版本头已同步到 `1.0.30`

## 变更说明

- 在中央 `AGENTS.md`、`docs/USAGE.md` 与受管 `WORKFLOW_CONTRACT` 中增加分支命名规则，要求 `codex/*` 分支名必须直接表达用途，避免 `help` / `fix` 这类模糊后缀。
- 更新下游 `AGENTS.md` 受管块模板和导出的 `WORKFLOW_CONTRACT` 模板，确保子仓在 apply `1.0.30` 后继承同一规则。
- 发布 `full_codex_flow@1.0.30`，并把当前中央 worktree 自身从旧的 `1.0.22` 安装状态补齐到 `1.0.30`，消除 entrypoint / commit gate 的受管漂移。

## 验证结果

- 命令：`python3 scripts/export_templates.py --repo-id AgentWorkflowKit`
  `python3 scripts/publish_release.py --profile full_codex_flow --version 1.0.30`
  `python3 scripts/apply_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-document-branch-naming --repo-id AgentWorkflowKit`
  `python3 scripts/check_release.py --repo-root /Users/pi/PyCharmProject/AgentWorkflowKit-wt-document-branch-naming --repo-id AgentWorkflowKit --json`
  `git diff --check`
  `rg -n 'codex/<purpose>|codex/help|codex/fix' AGENTS.md docs/USAGE.md .workflow-kit/WORKFLOW_CONTRACT.md templates/full_codex_flow/blocks/agents-workflow.md.tmpl templates/full_codex_flow/files/.workflow-kit/WORKFLOW_CONTRACT.md.tmpl`
- 范围：中央仓文档、受管合同、下游模板、release 工件，以及当前中央 worktree 的受管 runtime 对齐状态。
- 结果：`export_templates` 成功；`publish_release` 成功生成 `full_codex_flow@1.0.30`；`apply_release` 已把当前 worktree 安装版本同步到 `1.0.30`；`check_release` 返回 `status=current` 且 `mismatches=[]`；`git diff --check` 通过；`rg` 确认规则已进入中央 `AGENTS.md`、`docs/USAGE.md`、受管合同与导出模板。
- 未覆盖项：未实际执行下游 `scripts/apply_downstreams.py`；未新增 runtime 校验；历史上已经存在的模糊分支名不会被自动重命名。
- 提交快照：7e0c690a441865ec32783ea2b4c178771661953d2db99a689a5775c00de984d0

## 审查结果

- 审查方式：人工 diff 审查，重点检查 `AGENTS.md`、`docs/USAGE.md`、`./.workflow-kit/WORKFLOW_CONTRACT.md`、模板导出结果、`profiles/full_codex_flow/release.json` 与 `profiles/full_codex_flow/managed-files.lock.json`。
- 结论：未发现阻断问题。规则文本已经同时落到中央文档、受管合同和下游模板，release / install 版本也已对齐到 `1.0.30`。
- 残余风险：该规则目前仍是约定而非脚本校验；下游仓库需要在后续 apply `1.0.30` 后才会继承；`check_release` 里的 `AGENTS.md` redundancy warning 为既有提示，未在本次处理。
- 提交快照：7e0c690a441865ec32783ea2b4c178771661953d2db99a689a5775c00de984d0

## 完成待办项

- 无

## 当前占用待办项

- 无

## 风险与回滚

- 主要风险是本次提交同时包含“命名规则落地”和“中央 worktree 从 `1.0.22` 补齐到 `1.0.30` 的受管基线同步”。如需回滚，可直接回退本次提交，或重新发布并 apply 上一个稳定 release。
